{
  "bridge_gateway": "{{ agent_bridge_gateway }}",
  "bridge_subnet": "{{ agent_bridge_subnet }}",
  "squid_port": {{ squid_http_port }},
  "broker_port": {{ credential_broker_port }},
  "git_port": {{ git_remote_port }},
  "mcp_ports": {
    "fetch": {{ mcp_fetch_port }},
    "filesystem": {{ mcp_filesystem_port }}
  },
  "runs_dir": "/srv/runs",
  "git_mirror_dir": "{{ git_mirror_dir }}",
  "broker_tokens_dir": "{{ broker_tokens_dir }}",
  "allowed_domains_file": "{{ allowed_domains_file }}",
  "squid_access_log": "{{ squid_access_log }}"
}
